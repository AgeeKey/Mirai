# 🤖 MIRAI Локальный ИИ-Инженер - Быстрый Старт

## Что это?

MIRAI — это полностью автономная ИИ-система, которая может:
- 📋 Планировать задачи
- 🔍 Искать и читать информацию
- 💻 Писать и исправлять код
- 🐳 Запускать скрипты в безопасной песочнице (Docker)
- 📝 Документировать шаги
- 🧠 Учиться на своих ошибках

**Всё это работает локально, без внешних API (при использовании Ollama), быстро и безопасно.**

## Целевые показатели (KPI)

| Метрика | Цель | Текущий статус |
|---------|------|----------------|
| Успешность задач | ≥70% | Отслеживается |
| Время до результата | <10 мин | Отслеживается |
| Точность вызова инструментов | ≥90% | Отслеживается |
| Галлюцинации | →0% | Отслеживается |
| RAG hit@5 | >80% | Отслеживается |

## Установка за 5 минут

### 1. Установите зависимости

```bash
cd mirai-agent

# Основные зависимости
pip install -r requirements.txt

# Опционально: FAISS для локального RAG
pip install faiss-cpu sentence-transformers

# Опционально: Парсинг документов
pip install beautifulsoup4 pypdf
```

### 2. Установите Ollama (локальный LLM, опционально)

```bash
# Linux/Mac
curl -fsSL https://ollama.com/install.sh | sh

# Скачайте модель
ollama pull mistral:7b

# Проверьте
ollama list
```

### 3. Установите Docker (безопасное выполнение кода, опционально)

```bash
# Linux
curl -fsSL https://get.docker.com | sh

# Проверьте
docker --version
```

### 4. Запустите Dashboard

```bash
cd mirai-agent
python3 dashboard_server.py

# Откройте http://localhost:5000
```

## Основные компоненты

### 1. 🧠 Ollama Client - Локальный LLM

Работает без интернета, не требует OpenAI API.

```python
from core.ollama_client import OllamaClient

client = OllamaClient(model="mistral:7b")
response = client.generate("Что такое Python?")
print(response)
```

### 2. 📚 FAISS RAG - Локальная база знаний

Быстрый поиск по документам без API вызовов.

```python
from core.faiss_rag import FAISSRAGSystem

rag = FAISSRAGSystem()

# Индексируем базу знаний
rag.add_documents_from_directory("knowledge_base/", "*.md")

# Ищем
results = rag.search("Как использовать MIRAI?", top_k=5)
for doc, score in results:
    print(f"{score:.3f}: {doc['text'][:100]}...")
```

### 3. 🐳 Docker Runner - Безопасное выполнение

Код запускается в изолированных контейнерах без доступа к сети.

```python
from core.docker_runner import DockerCodeRunner

runner = DockerCodeRunner(
    memory_limit="256m",  # Ограничение памяти
    timeout=30  # Таймаут
)

result = runner.execute_python("""
print("Hello from Docker!")
x = 2 + 2
print(f"2 + 2 = {x}")
""")

print(f"Статус: {result.status.value}")
print(f"Вывод: {result.output}")
```

### 4. 🌐 Browser Automation - Парсинг веба

Получение информации из интернета.

```python
from core.browser_automation import BrowserAutomation

browser = BrowserAutomation()

# Получить текст страницы
text = browser.get_page_text("https://example.com")

# Поиск в Google
results = browser.search_google("Python учебники", num_results=10)
for r in results:
    print(f"{r['title']}: {r['url']}")
```

### 5. 🤖 ReAct Controller - Автономное выполнение

Агент сам планирует и выполняет задачи.

```python
from core.react_controller import ReActController, Task
from core.autonomous_agent import AutonomousAgent

agent = AutonomousAgent()
controller = ReActController(agent.client, agent.tools)

# Регистрируем инструменты
controller.register_tool_handler("execute_python", agent.execute_python)
controller.register_tool_handler("search_web", agent.search_web)

# Создаём задачу
task = Task(
    id="task_1",
    description="Напиши Python скрипт для вычисления чисел Фибоначчи",
    goal="Создать и протестировать скрипт",
    max_steps=10
)

# Выполняем
success, steps, answer = controller.execute_task(task)
print(f"Успех: {success}")
print(f"Ответ: {answer}")
```

### 6. 📝 Self-Reflection - Обучение на ошибках

Система запоминает, что работает, а что нет.

```python
from core.self_reflection import SelfReflectionSystem

system = SelfReflectionSystem()

# Добавляем рефлексию
system.add_reflection(
    task_id="task_1",
    task_description="Вычисление Фибоначчи",
    status="success",
    what_worked="Рекурсивный подход сработал хорошо",
    what_failed="",
    lessons_learned="Рекурсия подходит для малых чисел",
    tool_calls=[{"tool": "execute_python", "args": {}}],
    execution_time=5.2,
    tokens_used=150,
    hallucination_detected=False
)

# Получаем метрики
metrics = system.calculate_metrics()
print(f"Успешность: {metrics.task_success_rate:.1f}%")
print(f"Среднее время: {metrics.mean_time_to_result:.2f} мин")
```

## Dashboard API

### Запустить задачу

```bash
curl -X POST http://localhost:5000/api/tasks/run \
  -H "Content-Type: application/json" \
  -d '{
    "description": "Создай Python скрипт для проверки простых чисел",
    "goal": "Написать, протестировать и документировать функцию",
    "max_steps": 10
  }'

# Ответ:
# {
#   "success": true,
#   "task_id": "task_abc123",
#   "message": "Задача успешно запущена"
# }
```

### Проверить статус

```bash
curl http://localhost:5000/api/tasks/task_abc123

# Ответ:
# {
#   "success": true,
#   "task": {
#     "task_id": "task_abc123",
#     "status": "completed",
#     "steps": [...],
#     "answer": "Скрипт создан и протестирован",
#     "execution_time": 45.2
#   }
# }
```

### Список задач

```bash
curl http://localhost:5000/api/tasks/list
```

### Остановить задачу

```bash
curl -X POST http://localhost:5000/api/tasks/task_abc123/stop
```

### Получить метрики

```bash
curl http://localhost:5000/api/reflections/metrics

# Ответ:
# {
#   "metrics": {
#     "task_success_rate": 75.5,
#     "mean_time_to_result": 8.2,
#     "hallucination_rate": 2.1,
#     "tool_use_accuracy": 92.3
#   }
# }
```

## Типовые задачи (из постановки)

### 1. План спринта

```python
task = Task(
    description="Прочитай /knowledge, выпиши план спринта на неделю",
    goal="Создать список задач с критериями приёмки",
    max_steps=10
)
success, steps, answer = controller.execute_task(task)
```

### 2. Поиск точек отказа

```python
task = Task(
    description="Найди в репо точки отказа скриптов, предложи патч",
    goal="Найти проблемы и создать исправления",
    max_steps=15
)
success, steps, answer = controller.execute_task(task)
```

### 3. Сбор источников

```python
task = Task(
    description="Собери источники по машинному обучению",
    goal="Сделать конспект + список TODO-фич",
    max_steps=12
)
success, steps, answer = controller.execute_task(task)
```

### 4. Генерация прототипа

```python
task = Task(
    description="Сгенерь прототип скрипта для парсинга CSV",
    goal="Запустить в контейнере, приложить лог",
    max_steps=10
)
success, steps, answer = controller.execute_task(task)
```

## Тестирование

```bash
cd mirai-agent

# Запустить все интеграционные тесты
python3 test_integration.py

# Результат:
# ============================================================
# 📊 Test Results Summary
# ============================================================
#   Tool Validator       ✅ PASSED
#   FAISS RAG            ✅ PASSED
#   Docker Runner        ✅ PASSED
#   Self-Reflection      ✅ PASSED
#   ReAct Controller     ✅ PASSED
#   Browser Automation   ✅ PASSED
# ============================================================
# Total: 6/6 tests passed
```

## Архитектура решения

### Этап S-1: Мозг (LLM) ✅
- Ollama для локальной работы
- OpenAI для облачной работы
- Валидатор JSON-вызовов инструментов
- **Доказательство**: 5/5 тестов валидации прошли

### Этап S-2: RAG (База знаний) ✅
- FAISS для быстрого поиска
- Локальные эмбеддинги (sentence-transformers)
- Автоматическая индексация knowledge/
- Hit@k метрики
- **Доказательство**: Индексация и поиск работают

### Этап S-3: Code Runner (Выполнение) ✅
- Docker с изоляцией сети
- Ограничения памяти и CPU
- Поддержка Python, JS, Bash, Go, Rust
- Fallback на subprocess
- **Доказательство**: Код выполняется в изоляции

### Этап S-4: UI (Веб-панель) ✅
- Dashboard с API endpoints
- SSE для live-логов
- Список задач и статусы
- Кнопки старт/стоп
- **Доказательство**: API endpoints работают

### Этап S-5: Самоулучшение ✅
- SQLite для рефлексий
- Отслеживание что работает/не работает
- KPI метрики
- Экспорт для LoRA-тренировки
- **Доказательство**: Рефлексии сохраняются

## KPI - Доказательства

### Task Success Rate (Целевая: ≥70%)
```python
metrics = reflection_system.calculate_metrics()
print(f"Успешность: {metrics.task_success_rate:.1f}%")
# Отслеживается в /api/reflections/metrics
```

### Mean Time To Result (Целевая: <10 мин)
```python
print(f"Среднее время: {metrics.mean_time_to_result:.2f} мин")
# Отслеживается автоматически
```

### Tool-Use Accuracy (Целевая: ≥90%)
```python
accuracy = validator.get_accuracy()
print(f"Точность инструментов: {accuracy:.1f}%")
# Валидатор проверяет каждый вызов
```

### Hallucination Rate (Целевая: →0%)
```python
print(f"Галлюцинации: {metrics.hallucination_rate:.1f}%")
# Отслеживается в рефлексиях
```

### RAG Hit@k (Целевая: >80%)
```python
print(f"Hit@5: {rag.get_hit_rate(5):.1f}%")
# Метрики FAISS
```

## Безопасность

1. ✅ **Изоляция Docker**: Код без доступа к сети
2. ✅ **Ограничения памяти**: Защита от утечек
3. ✅ **Таймауты**: Защита от зависаний
4. ✅ **Валидация**: Проверка всех вызовов
5. ✅ **Песочница**: Нет прямого доступа к ФС

## Решение проблем

### Ollama не подключается
```bash
# Запустить Ollama
ollama serve

# Или через systemd
systemctl start ollama
```

### Docker: permission denied
```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### Chrome не найден
```bash
# Установить Chromium
sudo apt install chromium-browser
```

### FAISS не установлен
```bash
# Установить FAISS
pip install faiss-cpu sentence-transformers
```

## Что дальше?

1. **LoRA fine-tuning**: Обучение на собранных примерах
2. **Мультиагентность**: Несколько агентов работают вместе
3. **Проактивность**: Агент сам предлагает улучшения
4. **Автотесты**: Генерация тестов для кода
5. **Security scan**: Поиск уязвимостей

## Поддержка

- **Issues**: [GitHub Issues](https://github.com/AgeeKey/Mirai/issues)
- **Документация**: [LOCAL_AI_ENGINEER.md](LOCAL_AI_ENGINEER.md)
- **Dashboard**: http://localhost:5000

---

**Построено с 🤖 KAIZEN и 🌸 MIRAI**
