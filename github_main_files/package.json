{
  "name": "copilot-auto-accept",
  "displayName": "Copilot Auto Accept ⚡",
  "description": "🔥 EXTREME: Автоматическое принятие подсказок GitHub Copilot",
  "version": "1.0.0",
  "publisher": "extreme-dev",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Other"],
  "keywords": ["copilot", "auto", "accept", "extreme", "automation"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilotAutoAccept.toggle",
        "title": "Copilot Auto Accept: Toggle On/Off"
      },
      {
        "command": "copilotAutoAccept.forceAccept",
        "title": "Copilot Auto Accept: Force Accept Now"
      }
    ],
    "configuration": {
      "title": "Copilot Auto Accept",
      "properties": {
        "copilotAutoAccept.enable": {
          "type": "boolean",
          "default": true,
          "description": "🔥 Включить автоматическое принятие подсказок Copilot"
        },
        "copilotAutoAccept.idleMs": {
          "type": "number",
          "default": 800,
          "description": "⏱️ Задержка (мс) перед авто-принятием после остановки ввода"
        },
        "copilotAutoAccept.maxFileSizeKB": {
          "type": "number",
          "default": 500,
          "description": "📁 Максимальный размер файла (KB) для авто-принятия"
        },
        "copilotAutoAccept.strategy": {
          "type": "string",
          "enum": ["first", "stable"],
          "default": "stable",
          "description": "🎯 Стратегия: first = мгновенно, stable = с задержкой"
        },
        "copilotAutoAccept.languages": {
          "type": "array",
          "default": ["typescript", "javascript", "python", "go", "rust", "java", "csharp"],
          "description": "🔤 Языки для авто-принятия"
        },
        "copilotAutoAccept.excludePatterns": {
          "type": "array",
          "default": ["*.md", "*.txt", "*.json"],
          "description": "🚫 Исключить файлы по паттернам"
        },
        "copilotAutoAccept.smartGuards": {
          "type": "boolean",
          "default": true,
          "description": "🛡️ Умная защита от автоподстановки в незакрытых конструкциях"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./dist/test/runTest.js",
    "package": "vsce package",
    "install-local": "code --install-extension copilot-auto-accept-1.0.0.vsix"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^6.4.0",
    "@typescript-eslint/parser": "^6.4.0",
    "eslint": "^8.47.0",
    "typescript": "^5.1.6",
    "@vscode/vsce": "^2.19.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/copilot-auto-accept"
  },
  "license": "MIT"
}